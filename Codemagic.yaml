workflows:
  flutter-test:
    name: Flutter Test Workflow
    instance_type: mac_mini
    max_build_duration: 60
    environment:
      flutter: stable
    scripts:
      - name: Run tests
        script: flutter test
